name: personalise-cards
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/personalise_cards.py
jobs:
  personalise:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - name: Backfill persona examples
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scripts/personalise_cards.py
      - name: Commit and push
        run: |
          git config user.name "neuroitch-cards-bot"
          git config user.email "cards-bot@neuroitch.local"
          git add cards.json
          git diff --cached --quiet && { echo "nothing to commit"; exit 0; }
          git commit -m "Personalise: persona examples backfilled"
          git pull --rebase origin main
          git push origin main
